# Use DATABASE_URL instead of the individual DB_* variables when preferred.
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/pecausas

# Used when DATABASE_URL is not set.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pecausas
DB_USER=postgres
DB_PASSWORD=change-me

# Required for signed Flask sessions outside local development.
SESSION_SECRET_KEY=change-me

# Development-only: when true, skips PostgreSQL for view data and renders V2 templates with
# their approved fallback/demo content. Must be false (or unset) in production.
FRONTEND_DEMO_MODE=true